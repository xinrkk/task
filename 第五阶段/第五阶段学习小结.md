# 第五阶段学习小结
## go语言

- 语法简单：只有几十个关键字，循环只有for，变量声明用:=，容易上手。

- 并发方便：用go关键字启动goroutine，用channel通信，比传统多线程简单。

- 代码规范：gofmt统一格式，未使用的变量和包会报错，团队协作方便。

- 部署容易：直接编译成单个可执行文件，无依赖，跨平台。

- 工具齐全：自带测试、依赖管理、格式化工具。

优点：适合写网络服务、API，开发效率高。
缺点：错误处理较原始，泛型支持有限。
适合场景：云计算、中间件、后端服务。